# Stripe secret key (test or live). Required for all payment functions.
STRIPE_SECRET_KEY=sk_test_xxx
# Webhook signing secret (from `stripe listen` or the Dashboard endpoint).
STRIPE_WEBHOOK_SECRET=whsec_xxx
# Money Pull Up commission as a fraction (0.10 = 10%). Optional, defaults to 0.10.
COMMISSION_PERCENT=0.10
